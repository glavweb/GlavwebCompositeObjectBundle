{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% set field = field_description.options.field %}
{% set value = admin.getValueByInstanceField(object, field) %}

{% block field %}
    {% if value is not null %}
        {{ admin.toStringValueData(value) }}
    {% else %}
        -
    {% endif %}
{% endblock %}